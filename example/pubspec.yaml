name: flutter_csd_test_example
description: "Shows the window decoration widgets, in the style of each desktop."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.0-285.0.dev

dependencies:
  flutter:
    sdk: flutter

  flutter_csd_test:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
