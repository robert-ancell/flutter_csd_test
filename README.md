Test project showing window decoration widgets.

The window decoration widgets themselves are a package in `lib/`. The
application that shows them off is in `example/`; run it from there:

```sh
cd example
flutter run
```
